{
    "buildCommand":"cd client && npm install && CI='' npm run build",
    "outputDirectory":"client/dist",
    "rewrites":[
        {
            "source":"/(.*)",
            "destination":"/server/index.js"
        }
    ]
}
