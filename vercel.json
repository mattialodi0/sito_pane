{
    "buildCommand":"cd client && npm insatall && CI='' npm run build",
     "outputDirectory":"client/dist",
    "rewrites":[
        {
            "source":"/server/(.*)",
            "destination":"/server/index.js"
        }
    ]
}
