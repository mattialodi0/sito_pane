{
    "buildCommand":"cd client && CI='' npm run build",
     "outputDirectory":"client/dist",
    "rewrites":[
        {
            "source":"/server/(.*)",
            "destination":"/server/index.js"
        }
    ]
}
